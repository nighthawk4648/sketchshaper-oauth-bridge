{
  "name": "sketchshaper-oauth-bridge",
  "version": "1.0.0",
  "description": "OAuth callback bridge for SketchShaper Pro",
  "main": "api/callback.js",
  "scripts": {
    "build": "echo \"Skipping build step — not needed\""
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
